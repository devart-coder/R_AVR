[workspace]
members = [
    "nano",
    "blink",
    "main"
]
default-members = ["main"]

resolver = "3"
[profile.dev]
panic = "abort"
lto = true
opt-level = "s"

[profile.release]
panic = "abort"
lto = true
opt-level = "s"