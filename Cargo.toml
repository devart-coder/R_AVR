# [workspace]
# members = [
    # ".",
    # "nano",
# ]
[package]
name = "AVR"
version = "0.1.0"
edition = "2024"
[profile.dev]
panic = "abort"
lto = true
opt-level = "s"

[profile.release]
panic = "abort"
lto = true
opt-level = "s"
[dependencies]
nano = { path = "./nano" }
